# 网站内容管理指南

## 📝 概述

本项目已配置为**集中式内容管理**，您只需修改 `_data/site-content.yml` 文件即可更新网站的主要内容，无需逐个修改多个文件。

## 🚀 快速开始

### 1. 主要配置文件

**`_data/site-content.yml`** - 这是您主要需要编辑的文件，包含：

- 网站基本信息（标题、描述、作者）
- 关于页面内容
- 导航菜单配置
- 社交媒体链接
- 各页面设置

### 2. 配置文件结构

```yaml
site_info:          # 网站基本信息
about:              # 关于页面内容
portfolio:          # 作品集设置
navigation:         # 导航菜单
social:             # 社交链接
gallery:            # 图库设置
search:             # 搜索设置
page_titles:        # 其他页面标题
blog:               # 博客设置
theme:              # 主题设置
```

## 📂 文件说明

### 核心配置文件

| 文件 | 说明 | 使用频率 |
|------|------|----------|
| `_data/site-content.yml` | **主要内容配置** | ⭐⭐⭐⭐⭐ 经常修改 |
| `_config.yml` | Jekyll 技术配置 | ⭐⭐ 偶尔修改 |
| `_data/social.yml` | 社交链接详细配置 | ⭐⭐⭐ 有时修改 |
| `_data/language.yml` | 界面语言文字 | ⭐ 很少修改 |

### 内容文件夹

| 文件夹 | 说明 |
|--------|------|
| `_posts/` | 博客文章（Markdown 文件） |
| `_portfolio/` | 作品集项目（Markdown 文件） |
| `assets/img/` | 图片资源 |
| `pages/` | 静态页面 |

## 🔧 常见修改指南

### 修改网站标题和描述

编辑 `_data/site-content.yml`：

```yaml
site_info:
  title: "您的网站标题"
  description: "您的网站描述"
```

### 修改关于页面

```yaml
about:
  title: "关于"
  content: |
    ## 您的标题

    您的内容（支持 Markdown 格式）

    ### 子标题

    更多内容...
```

### 添加导航菜单项

```yaml
navigation:
  - title: "新页面"
    url: "/new-page/"
    icon: "fa-star"
    position: 8
```

### 添加社交媒体链接

```yaml
social:
  github:
    username: "your-username"
    enabled: true
```

### 添加新作品集项目

1. 在 `_portfolio/` 文件夹中创建新的 `.md` 文件
2. 在 `site-content.yml` 中添加项目信息

```yaml
portfolio:
  projects:
    - name: "新项目名称"
      description: "项目描述"
      category: "分类"
```

### 添加博客文章

1. 在 `_posts/` 文件夹中创建新文件
2. 文件命名格式：`YYYY-MM-DD-标题.md`
3. 文件内容格式：

```markdown
---
layout: post
title: "文章标题"
date: 2026-02-10
categories: [分类]
tags: [标签1, 标签2]
feature-img: "assets/img/your-image.jpg"
---

文章内容...
```

## 🎨 主题自定义

### 更改颜色主题

```yaml
theme:
  color_theme: "dark"  # 可选：auto, dark, light
```

### 启用/禁用功能

```yaml
theme:
  katex: true        # 数学公式渲染
  mermaid: "default" # 图表渲染
```

## 📱 社交链接配置

### 可用的社交平台

在 `_data/social.yml` 中配置（或使用 `_data/site-content.yml` 中的简化配置）：

- GitHub
- LinkedIn
- Twitter/X
- 微信
- 邮箱
- 更多...

## 🖼️ 图片管理

### 推荐的图片存放位置

```
assets/
├── img/
│   ├── header/          # 头部背景图
│   ├── pexels/          # 图库图片
│   ├── profile/         # 个人照片
│   └── projects/        # 项目截图
├── favicon.ico          # 网站图标
└── ...
```

### 图片使用示例

```yaml
site_info:
  avatar: "assets/img/profile/avatar.jpg"
  header_feature_image: "assets/img/header/banner.jpeg"
```

## ⚙️ 高级配置

### 修改 _config.yml

`_config.yml` 包含 Jekyll 的技术配置，通常不需要频繁修改：

- `baseurl`: 网站子路径
- `url`: 网站域名
- `plugins`: Jekyll 插件

### 自定义样式

如需自定义 CSS 样式，编辑：
- `_sass/base/_variables.scss` - 变量定义
- `_sass/base/_base.scss` - 基础样式

## 📋 检查清单

更新网站时的检查清单：

- [ ] 更新 `_data/site-content.yml` 中的基本信息
- [ ] 检查关于页面内容
- [ ] 更新作品集项目
- [ ] 添加新的博客文章
- [ ] 更新社交媒体链接
- [ ] 检查图片资源
- [ ] 本地预览效果
- [ ] 提交到 Git 仓库

## 🔄 工作流程

### 本地预览

```bash
# 安装依赖
bundle install

# 启动本地服务器
bundle exec jekyll serve

# 浏览器访问
# http://localhost:4000
```

### 部署到 GitHub Pages

1. 推送到 GitHub 仓库
2. GitHub Pages 会自动构建和部署

## 📞 获取帮助

- Jekyll 文档: https://jekyllrb.com/docs/
- Type on Strap 主题: https://github.com/sylhare/Type-on-Strap
- Markdown 语法: https://www.markdownguide.org/

## 📝 备注

- 所有 YAML 文件使用**2空格缩进**
- Markdown 内容使用 `|` 符号进行多行文本块
- 文件路径使用正斜杠 `/`（即使在 Windows 上）
- 修改后需要重新构建网站才能看到效果
